1、目前我自己的项目的docker 镜像会实现RQALPHA_BUNDLE_URL 包的下载。 包括两个部分，一个是首次安装的时候，会下载。另一个是镜像的crontab 定时任务，每个月第一天会下载。 我本地目录是/home/app/.rqalpha/bundle，volume 挂在容器对应的位置是 /data/rqalpha/bundle 。

2、我需要在新增一个"行情数据管理"的功能，前端放在导航栏研究工作台后面。 点击进去之后，包含子页面：

数据全景：包含数据下载时间，总文件数、总大小、最近修改时间，各行情条数（股票、基金、期货、指数等）这里，后端flask 需要完成分析的程序，使用rqalpha 库，解析bundle目录中的文件，统计数据写入数据库。 这个分析的程序，有两种触发方式，一种是解压后自动触发，另一种是数据全景页面提供一个按钮触发。

手动下载：点击按钮触发下载更新流程。下载需要判断本地目录，是否已经存在已解压的文件，如果已存在当月已解压的文件，则弹框提醒，文件已是最新，不要重复下载。用户确认以后才可以下载。 如果已解压的文件的最近修改时间是上个月或者之前，则直接下载。默认采用rqalpha update-bundle 增量更新，数据直接追加到bundle的文件里，注意目录不要搞错了。 要显示下载进度，用进度条显示，增量下载如果没有下载进度接口，可以参考已有的全量下载的接口处理。注意增量更新后，要触发数据全景的分析的程序

页面提供一个全量下载的按键，点击之后，要判断本地目录，是否已经存在已解压的文件，如果已存在当月已解压的文件，则弹框提醒，文件已是最新，不要重复下载。用户确认以后才可以下载。 如果已解压的文件的最近修改时间是上个月或者之前，则直接下载。注意，全量下载与增量下载流程不一样，多了解压的操作。 全量下载、解压的流程与首次安装一样，显示下载进度，复用已有的接口，用进度条显示。 下载之后，要解压，也显示进度条。下载的流程和目录复用首次安装的时候的配置。注意全量更新后，要触发数据全景的分析的程序

下载配置：将镜像的crontab 定时任务，在这里实现配置可视化。需要显示和记录运行日志。注意任务完成后，要触发数据全景的分析的程序

3、不要影响现有的登录、回测工作台、研究工作台的显示，后端逻辑。
